# Handoff — 2026-05-27a

## Next session focus

**Execute the approved plan `PLN1` — rebuild the static `htmlsite/` site as an idiomatic Astro 6 demo.** All four planning gates are approved and execution is authorized; nothing more to plan. **Start here:** open `.docs/plans/PLN1-20260527-astro-pandaplay-rebuild-index.md` and read its **"Resume note for the execution session"** (in the *Execution authorization* section) — it is the literal startup checklist. Then establish the pre-execution baseline and begin the Delivery slices.

First concrete action: run `pnpm exec astro check` + `pnpm build` on the current starter to record the pre-execution baseline in the plan's *Execution notes / deviations*, then start Delivery step 1 (setup).

This serves the **Execution → Closeout** phase. The plan is the spec; build to it, don't re-design.

Context for *why* this matters: the site is a teaching demo for a live Astro presentation. Visual fidelity to `htmlsite/` and *idiomatic, readable* Astro patterns both matter — the audience will read the code.

## Required reading before starting (in order)

1. `.docs/plans/PLN1-20260527-astro-pandaplay-rebuild-index.md` — **the plan; source of truth.** Read in full. Start with the *Execution authorization → Resume note*, then *Delivery — order of work*, then the *Acceptance checkpoint*.
2. `~/.claude/skills/planning-system/standards/PLANNING-DELIVERY.md` — execution + closeout rules (slice gate, deviation recording, commit cadence, closeout). **The repo is NOT bootstrapped** with `.docs/standards/`, so read from the skill's bundled `standards/` folder.
3. `~/.claude/skills/planning-system/standards/VERIFICATION.md` — governs the Acceptance checks you'll run before handoff.
4. `htmlsite/index.html`, `catalog.html`, `bamboo-jungle-gym.html`, `roly-poly-slide.html` — **read first-hand for markup fidelity.** Do not rebuild from the plan's prose alone; the plan references the source by location rather than reproducing it.

Authority: the plan index governs *this work*; if it conflicts with the standards, the plan's recorded decisions (with their approval records) win for this plan. `~/.claude/CLAUDE.md` governs working style and commit discipline globally.

## What happened this session

This was a **planning-only** session — no code written, no commits. `git log` is unchanged from the initial commit; the only new artifact is `.docs/` (the plan + this handoff), currently untracked.

We ran the full `planning-system` gate sequence (Intent → Approach+Delivery → Acceptance), each with sub-agent review. Context not captured elsewhere:

- **One thing was prototyped, not just designed:** `z.array(image())` in a content-collection schema was build-tested against this repo's Astro 6.3.8 and **confirmed working** (gallery images optimize to webp). That throwaway test (`_galtest/`) was created and **removed** — it should not exist; if you see it, delete it. The finding is recorded in the plan.
- **`sharp` is mandatory:** the prototype proved the build fails with `MissingSharp` without it in this pnpm setup. The plan makes `pnpm add sharp` a required setup step, not conditional.

## Settled inputs carried forward (do NOT re-ask)

These are decided and recorded in the plan with approval records. Do not re-litigate:

- **Styling:** Tailwind v4 via `@tailwindcss/vite` (`astro add tailwind`); discard the pre-compiled `htmlsite/tailwind_theme/tailwind.css`.
- **Fonts:** Astro 6 **Fonts API** (`experimental.fonts`, Google provider, self-hosted) for Nunito Sans + Quicksand — **not** a CDN `@import`. `@theme inline` in `global.css` maps `--font-sans`/`--font-display` to the Astro font vars.
- **Content:** Astro Content Collections; all 6 products as entries with a `status` flag (2 available, 4 coming-soon); `[slug].astro` via `getStaticPaths` over available entries.
- **Images:** `astro:assets` `<Image />` for all photos (moved to `src/assets/`).
- **Animations:** `motion` as an npm dep, loaded from one shared `<script>` in `BaseLayout`; `[data-hide]{opacity:0}` flicker-prevention rule in `global.css`; drop the unused `stagger` import.
- **Layouts:** exactly two — `BaseLayout` + `ProductLayout` (which nests `BaseLayout`).
- **Components:** full prop-driven kit (Button, Badge, ProductCard, FeatureCard, StatCard, SectionHeading, Seo, Nav, Footer, icons/*).
- **SEO:** hand-rolled `Seo.astro` (title/description/canonical/OG/Twitter), wired into `BaseLayout`. Standard tag set, no SEO package.
- **Dynamic island:** Preact (`astro add preact`) catalog filter, `client:visible`; Astro renders cards, island toggles `hidden` by `data-status`.
- **SSG+SSR:** keep `output: 'static'`; add `@astrojs/node` (`mode: 'standalone'`); one `/quote` route with `prerender = false`; detail-page "Request a Quote" buttons link to it.
- **TypeScript:** stays on `astro/tsconfigs/strict` (already configured — `tsconfig.json` untouched). User said `strict`, not `strictest`.
- **Vite pinned to `^7`** via `pnpm.overrides` (NOT v8).
- **CTA copy:** full `ctaHeading`/`ctaBody` schema fields (user overrode the templated-noun recommendation).
- **Coming-soon CTA:** non-interactive/disabled (intentional improvement over source's dead `href="#"`).
- **Execution mode:** main branch (build directly on `main` — user declined branch/worktree), guided, inline orchestrator (explicit override of the Sonnet-sub-agent default).
- **Commits:** every commit needs explicit approval — inspect staged diff, show exact message, wait. Commit and push are separate approvals. (User's global CLAUDE.md rule; do not chain.)
- **Acceptance bar:** build + dev + browser screenshots vs. `htmlsite/`.

## In-flight work

None in code. No uncommitted source edits, no half-applied refactors. The only working-tree change is the untracked `.docs/` directory (plan + this handoff). The codebase is the stock Astro starter (`src/pages/index.astro` is the default Astro page, to be replaced in Delivery step 6).

## Process notes

- **This planning session was "fast and loose"** (user's words) — I jumped the Intent gate (drafted the whole plan before Intent approval), asked HOW-questions at the Intent gate, and read `VERIFICATION.md` only when told to. The user caught each. The plan is sound *because* of those corrections, but the document carries the scar tissue: **ACs are numbered out of sequence** (AC3b, AC3c, AC9, AC10… interleaved) and there are inline `(resolves review Rx)` annotations and decision banners. This is cosmetic — content is final and approved. A structural cleanup pass was offered and **declined**; don't redo it unasked.
- **Build to the approved plan literally.** The failure mode this session was improvising ahead of the gate. In execution that maps to: don't add scope, don't swap approaches mid-build — if the plan says do X, do X; if X proves wrong, record a deviation and (if material) return to the user, per PLANNING-DELIVERY §17.
- **Context7 MCP is over quota** this session; I used `WebFetch` against `docs.astro.build` to verify APIs. If you need to re-verify an Astro API, WebFetch the official docs rather than relying on Context7.
- **Delivery order is deliberate:** demo features (island, SSR) come **last** as additive layers on a working SSG site, so a hiccup there can't block the core rebuild. Keep that ordering.
